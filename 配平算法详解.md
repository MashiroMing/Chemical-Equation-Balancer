# 化学方程式配平算法详解

## 整体思路

配平化学方程式的本质是**解齐次线性方程组**：

$$
a\text{A} + b\text{B} + c\text{C} = d\text{D} + e\text{E}
$$

约束条件：
- **质量守恒**：每种原子的数量，反应前 = 反应后
- **电荷守恒**（离子反应）：总电荷，反应前 = 反应后

程序从输入字符串到输出配平系数，共经过 **5 个阶段**：

```
输入 "Fe2+ + MnO4- + H+ = Fe3+ + Mn2+ + H2O"
                          │
         ┌────────────────┼────────────────┐
         ▼                ▼                ▼
   ① 解析化学式      ② 列守恒方程      ③ 构建矩阵
   数出每种原子      每原子一方程       转成数字表格
         │                │                │
         └────────────────┼────────────────┘
                          ▼
                   ④ 高斯消元 (RREF)
                   化为最简行阶梯形
                          │
                          ▼
                   ⑤ 提取整数系数
                   5, 1, 8, 5, 1, 4
```

---

## 阶段 1：解析化学式

**目标**：输入化学式字符串，输出每种原子的个数 + 电荷值。

### 1.1 原子计数（`parse_atoms`）

逐字符扫描，用**栈**处理括号嵌套。以 `(NH4)2SO4` 为例：

```
字符流:  ( → N → H → 4 → ) → 2 → S → O → 4

栈的视角:

第1层: {}                         ← 最外层

遇到 '(' → 压入新层:
第1层: {}
第2层: {}                         ← 括号内

遇到 'N', 'H', '4':
第2层: {N: 1, H: 4}

遇到 ')' + '2':
第2层弹出 → {N:1, H:4} × 2 = {N:2, H:8}
合并到第1层: {N:2, H:8}

遇到 'S', 'O', '4':
第1层: {N:2, H:8, S:1, O:4}      ← 最终结果
```

**为什么用栈？** 化学式括号嵌套是后进先出（LIFO）结构——这正是栈的天然应用场景。

### 1.2 电荷识别（`disambiguate_charge`）

程序需要区分 `CO32-` 中的 `2-` 是电荷还是下标。

**策略**：从字符串末尾向前试探每一种分割位置，用启发式规则选最优解。

```
CO32- 的所有可能分割：

分割位置 1: "C"  + "O32-"  → base="C",  charge=? (不合理)
分割位置 2: "CO" + "32-"   → base="CO", charge=-32 (不合理)
分割位置 3: "CO3" + "2-"   → base="CO3", charge=-2 ✅ 正确答案
```

**筛选规则**：
- 后缀必须匹配 `数字 + +/-` 模式
- 解析 base 部分，单个元素下标不超过 20
- 多元素化合物：原子数多的优先（更完整）
- 单元素：原子数少的优先（更简洁）

---

## 阶段 2：列守恒方程

**核心思想**：把每种原子看成一个等式，把电荷也当作一种"原子"。

以 `a Fe2+ + b MnO4- + c H+ → d Fe3+ + e Mn2+ + f H2O` 为例：

| 守恒项 | 左边 | 右边 | 方程（移项到左边） |
|--------|------|------|-------------------|
| Fe | a × 1 | d × 1 | a − d = 0 |
| Mn | b × 1 | e × 1 | b − e = 0 |
| O  | b × 4 | f × 1 | 4b − f = 0 |
| H  | c × 1 | f × 2 | c − 2f = 0 |
| 电荷 e⁻ | a×2 + b×(−1) + c×1 | d×3 + e×2 | 2a + b + c − 3d − 2e = 0 |

> **关键设计**：电荷被当作一个名为 `e⁻` 的虚拟元素。质量和电荷用完全相同的数学框架处理。

---

## 阶段 3：构建矩阵

将方程组的系数写成矩阵，右边化合物的系数取**负值**（移项）。

```
        a    b    c    d    e    f     (系数变量)
Fe  [   1    0    0   −1    0    0  ]  = 0
Mn  [   0    1    0    0   −1    0  ]  = 0
O   [   0    4    0    0    0   −1  ]  = 0
H   [   0    0    1    0    0   −2  ]  = 0
e⁻  [   2    1    1   −3   −2    0  ]  = 0
```

**为什么用 Fraction（分数）而不是小数？** 化学系数必须精确。`1/3 × 3 = 1`（分数），而 `0.333... × 3 = 0.999... ≠ 1`（浮点误差）。

---

## 阶段 4：高斯-约当消元（RREF）

将矩阵化为**行简化阶梯形**。三步循环：

```
① 选主元：在当前列找非零行，交换到上方
② 归一化：该行除以主元值，让主元 = 1
③ 消去：用该行把其他所有行的同一列消成 0
```

**可视化**：

```
初始:              选主元 & 交换:      归一化:           消去:
[2  1 −1]          [2  1 −1]          [1  0.5 −0.5]    [1  0.5 −0.5]
[4 −6  0]     →    [4 −6  0]     →    [4  −6    0] →   [0  −8    2]
[−2 7  2]          [−2 7  2]          [−2  7    2]     [0   8    1]

继续处理下一列 → 最终 RREF:
[1  0  a']
[0  1  b']
[0  0   0]
```

> **关键洞察**：化学配平是**齐次方程组**（右边全是 0），解空间至少有一个自由变量。因为如果 (a,b,c,d,e,f) 是解，那么 (2a,2b,2c,2d,2e,2f) 也是解（同比例放大）。

---

## 阶段 5：提取整数系数

### 5.1 求零空间

消元后，矩阵列分两种：

- **主元列**：有且仅有一个 1，其余为 0（约束变量）
- **自由列**：没有主元（自由变量）

设自由变量为 1，从下往上回代：

```
RREF 结果:
        a   b   c   d   e   f
Fe  [   1   0   0  −1   0   0  ]   ← a 是主元
Mn  [   0   1   0   0  −1   0  ]   ← b 是主元
O   [   0   0   1   0   0  −2  ]   ← c 是主元
H   [   0   0   0   1   0  −3  ]   ← d 是主元
e⁻  [   0   0   0   0   1  −5  ]   ← e 是主元

自由变量: f
设 f = 1，从下往上:

e − 5×1 = 0  →  e = 5
d − 3×1 = 0  →  d = 3
c − 2×1 = 0  →  c = 2
b − 1×1 = 0  →  b = 1
a − 1×1 = 0  →  a = 1

分数解: (1, 1, 2, 3, 5, 1)
```

### 5.2 转最简整数

若得到分数，通分后约简：

```
分数解: [1/2, 1, 3/2]
  ↓ 求分母 LCM = 2，全部 ×2
[1, 2, 3]
  ↓ 求 GCD = 1，已最简
最终系数: 1, 2, 3
```

---

## 完整流程示意

```
┌─────────────────────────────────────────────────────┐
│  输入: "Fe2+ + MnO4- + H+ = Fe3+ + Mn2+ + H2O"     │
└───────────────────────┬─────────────────────────────┘
                        ▼
┌─────────────────────────────────────────────────────┐
│ ① 解析化学式                                         │
│   Fe2+  → {Fe:1}, charge=+2                         │
│   MnO4- → {Mn:1, O:4}, charge=-1                    │
│   H+    → {H:1}, charge=+1                          │
│   Fe3+  → {Fe:1}, charge=+3                         │
│   Mn2+  → {Mn:1}, charge=+2                         │
│   H2O   → {H:2, O:1}, charge=0                      │
└───────────────────────┬─────────────────────────────┘
                        ▼
┌─────────────────────────────────────────────────────┐
│ ② 列守恒方程 + ③ 构建矩阵                             │
│        a   b   c   d   e   f                         │
│   Fe [ 1   0   0  −1   0   0 ]                       │
│   Mn [ 0   1   0   0  −1   0 ]                       │
│   O  [ 0   4   0   0   0  −1 ]                       │
│   H  [ 0   0   1   0   0  −2 ]                       │
│   e⁻ [ 2   1  −1  −3  −2   0 ]                       │
└───────────────────────┬─────────────────────────────┘
                        ▼
┌─────────────────────────────────────────────────────┐
│ ④ 高斯-约当消元 → RREF                                │
│   选主元 → 归一化 → 消去 → 重复直到最简                 │
└───────────────────────┬─────────────────────────────┘
                        ▼
┌─────────────────────────────────────────────────────┐
│ ⑤ 提取系数                                           │
│   设自由变量=1 → 回代 → 通分 → 约简                    │
│                                                      │
│   结果: 5Fe2+ + 1MnO4- + 8H+ = 5Fe3+ + 1Mn2+ + 4H2O │
└─────────────────────────────────────────────────────┘
```

---

## 设计亮点

| 特点 | 说明 |
|------|------|
| **统一处理质量 + 电荷** | 电荷作为 `e⁻` 虚拟元素纳入矩阵，一套框架解决两类守恒 |
| **精确分数运算** | `Fraction` 杜绝浮点精度误差，保证系数精确为有理数 |
| **通用性** | 只要方程有解就能配平，不受元素种类、系数范围限制 |
| **最简整数解** | LCM 通分 + GCD 约简，自动输出最简整数比 |
| **栈解析括号** | 栈数据结构天然适配化学式括号嵌套 |

## 与高中数学的衔接

- **线性方程组**：配平本质是解齐次线性方程组
- **矩阵与高斯消元**：RREF 是线性代数核心算法的直接应用
- **分数运算**：`Fraction` 对应有理数通分约分
- **栈**：括号匹配是栈的经典应用场景
